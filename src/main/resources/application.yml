spring:
  application:
    name: positive-ai-code-backend
server:
  port: 8080
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.xr.positiveaicode.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
